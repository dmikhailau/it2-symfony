{% extends 'base.html.twig' %}

{% block title %}Edit Seats{% endblock %}

{% block body %}
    <h1>Edit Seats</h1>

    {{ include('seats/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('seats_index') }}">back to list</a>

    {{ include('seats/_delete_form.html.twig') }}
{% endblock %}
