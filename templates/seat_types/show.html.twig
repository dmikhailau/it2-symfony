{% extends 'base.html.twig' %}

{% block title %}SeatTypes{% endblock %}

{% block body %}
    <h1>SeatTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seat_type.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ seat_type.title }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ seat_type.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('seat_types_index') }}">back to list</a>

    <a href="{{ path('seat_types_edit', {'id': seat_type.id}) }}">edit</a>

    {{ include('seat_types/_delete_form.html.twig') }}
{% endblock %}
