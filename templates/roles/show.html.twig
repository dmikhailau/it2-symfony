{% extends 'base.html.twig' %}

{% block title %}Roles{% endblock %}

{% block body %}
    <h1>Roles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ role.title }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ role.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('roles_index') }}">back to list</a>

    <a href="{{ path('roles_edit', {'id': role.id}) }}">edit</a>

    {{ include('roles/_delete_form.html.twig') }}
{% endblock %}
